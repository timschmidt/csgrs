[package]
name = "csgrs-ffi"
version = "0.1.0"
edition = "2024"
description = "C ABI for csgrs scalar adapters"
authors = ["Timothy Schmidt <timschmidt@gmail.com>"]
license = "MIT"
repository = "https://github.com/timschmidt/csgrs"
readme = "README.md"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
csgrs = { version = "0.23.0", path = "..", default-features = false, features = ["sketch"] }
hyperlattice = { version = "0.6.0", path = "../../hyperlattice" }
hyperreal = { version = "0.13.1", path = "../../hyperreal", default-features = false }
