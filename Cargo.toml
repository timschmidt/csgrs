[package]
name = "csgrs"
version = "0.23.0"
edition = "2024"
description = "Constructive solid geometry (CSG) on meshes composed from hyper geometry crates"
authors = ["Timothy Schmidt <timschmidt@gmail.com>"]
# msrv
rust-version = "1.95.0"

license = "MIT"
repository = "https://github.com/timschmidt/csgrs"
readme = "readme.md"

keywords = ["geometry", "physics", "csg", "slicing", "offsetting"]
categories = ["game-development", "graphics", "mathematics", "simulation", "wasm"]
autobenches = false

[package.metadata.docs.rs]
no-default-features = true
features = [
  "attributed",
  "curve",
  "stl-io",
  "dxf-io",
  "obj-io",
  "ply-io",
  "amf-io",
  "gltf-io",
  "vrml-io",
  "gerber-io",
  "svg-io",
  "image-io",
  "metaballs",
  "sdf",
  "offset",
  "truetype-text",
  "hershey-text",
  "wasm",
]

[lib]
crate-type = ["cdylib", "rlib"]

[[bench]]
name = "part_blueprint"
path = "benchmarks/rust/part_blueprint.rs"
harness = false
required-features = ["attributed"]

[[bench]]
name = "mesh_pipeline"
path = "benchmarks/rust/mesh_pipeline.rs"
harness = false

[[bench]]
name = "kernel_comparison"
path = "benchmarks/rust/kernel_comparison.rs"
harness = false
required-features = ["attributed"]

[[bench]]
name = "feature_pipeline"
path = "benchmarks/rust/feature_pipeline.rs"
harness = false
required-features = ["bench-feature-pipeline"]

[[bench]]
name = "profile_primitives"
path = "benchmarks/rust/profile_primitives.rs"
harness = false
required-features = ["curve"]

[[bench]]
name = "competitive"
path = "benchmarks/rust/competitive.rs"
harness = false

[[bench]]
name = "solidean_comparison"
path = "benchmarks/rust/solidean_comparison.rs"
harness = false

[[bench]]
name = "adapter_graphics_export"
path = "benchmarks/rust/adapter_graphics_export.rs"
harness = false

[profile.release]
opt-level = 3
lto = true
debug = false
debug-assertions = false
overflow-checks = false

[profile.embedded]
inherits = "release"
opt-level = "z"

[dependencies]
# geometry
#alum = "0.6.1"
hypercurve = { version = "0.3.1", path = "../hypercurve", features = ["triangulation"] }
hyperlattice = { version = "0.6.1", path = "../hyperlattice" }
hyperlimit = { version = "0.4.1", path = "../hyperlimit" }
hypermesh = { version = "0.1.0", path = "../hypermesh" }
hyperphysics = { version = "0.3.0", path = "../hyperphysics" }
hyperreal = { version = "0.13.1", path = "../hyperreal", default-features = false, features = ["cached-f64-approx"] }
hypersdf = { version = "0.2.0", path = "../hypersdf", optional = true }
hypertri = { version = "0.4.1", path = "../hypertri", default-features = false, features = ["earcut"] }
# bevy mesh conversion
bevy_mesh = { version = "0.18", optional = true }
bevy_asset = { version = "0.18", optional = true }
wgpu-types = { version = "27", optional = true, default-features = false }

# utils
thiserror = "2.0"
hashbrown = { version = "0.15" }
fast-surface-nets = { version = "0.2.1", optional = true }
base64 = { version = "0.22", optional = true }
# doc
#doc-image-embed = "0.2" # fork of https://crates.io/crates/embed-doc-image - should be upstreamed.

# io/load
chrono = { version = "0.4", optional = true }
dxf = { version = "0.6", optional = true }
stl_io = { version = "0.8", optional = true }
image = { version = "0.25", optional = true }
gerber-types = { version = "0.7.0", optional = true }
gerber_parser = { version = "0.4.0", optional = true }

# font
ttf-utils = { version = "0.1", optional = true, package = "ttf-parser-utils"}
ttf-parser = { version = "0.25", optional = true }

doc-image-embed = "0.2.1"

# wasm
wasm-bindgen = { version = "0.2", optional = true }
js-sys = { version = "0.3.81", optional = true }
serde-wasm-bindgen = { version = "0.6.5", optional = true }
serde_json = { version = "1.0.145", optional = true }
serde = { version = "1.0.228", optional = true }
gltf = { version = "1.4.1", optional = true, default-features = false, features = ["names", "utils"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
# DXF and Gerber depend transitively on uuid; wasm32 requires an explicit
# browser randomness provider even though csgrs does not construct UUIDs.
uuid = { version = "1.18", features = ["js"] }

# Keep transitive sibling crates unified while using local path development mode.
[patch.crates-io]
hyperlattice = { path = "../hyperlattice" }
hyperlimit = { path = "../hyperlimit" }
hyperreal = { path = "../hyperreal" }
hypertri = { path = "../hypertri" }
hypersolve = { path = "../hypersolve" }

[features]
default = ["curve", "stl-io", "dxf-io", "obj-io", "ply-io", "amf-io", "gltf-io", "vrml-io", "gerber-io", "image-io", "metaballs", "sdf", "truetype-text", "hershey-text"]
attributed = []
bench-feature-pipeline = [
  "attributed",
  "curve",
  "stl-io",
  "dxf-io",
  "obj-io",
  "ply-io",
  "amf-io",
  "gltf-io",
  "gerber-io",
  "svg-io",
  "image-io",
  "metaballs",
  "sdf",
  "offset",
  "truetype-text",
  "hershey-text",
  "bevymesh",
]
dispatch-trace = [
  "hypercurve/dispatch-trace",
  "hypermesh/dispatch-trace",
  "hyperreal/dispatch-trace",
]
curve = []
stl-io = [
  "stl_io",
]
svg-io = [
  "curve",
  "hypercurve/svg",
]
dxf-io = [
  "curve",
  "chrono",
  "dxf",
]
obj-io = [
  # OBJ format support - no external dependencies needed
]
ply-io = [
  # PLY format support - no external dependencies needed
]
amf-io = [
  # AMF format support - no external dependencies needed
]
gltf-io = [
  "base64",
  "serde_json",
  "dep:gltf",
]
vrml-io = [
  # VRML 2.0 geometry import - no external dependencies needed
]
gerber-io = [
  "curve",
  "gerber-types",
  "gerber_parser",
]
truetype-text = [
  "ttf-parser",
  "ttf-utils",
]
hershey-text = [
  "curve",
  "hypercurve/hershey",
]
image-io = [
  "image",
  "curve",
]
metaballs = [
  "fast-surface-nets",
]
sdf = [
  "fast-surface-nets",
  "hypersdf",
]
# Native Hypercurve offset and regularized-buffer operations.
offset = ["curve"]
bevymesh = [
  "dep:bevy_mesh",
  "dep:bevy_asset",
  "dep:wgpu-types"
]
wasm = [
  "curve",
  "svg-io",
  "stl-io",
  "amf-io",
  "gltf-io",
  "sdf",
  "wasm-bindgen",
  "js-sys",
  "serde-wasm-bindgen",
  "serde_json",
  "serde",
]

[[example]]
name = "readme_renders"
required-features = [
  "curve",
  "image-io",
  "truetype-text",
  "metaballs",
  "sdf",
  "offset",
]

[[example]]
name = "scalar_adapter"

[dev-dependencies]
boolmesh = "=0.1.9"
manifold-rust = "=0.9.2"
ply-rs = "0.1.3"
proptest = "1.9"
quick-xml = "0.39.4"
serde_json = "1.0.145"
three-d-asset = "=0.4.0"
tri-mesh = "=0.6.1"
