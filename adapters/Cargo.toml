[package]
name = "csgrs-adapter"
version = "0.1.0"
edition = "2024"
description = "Primitive scalar adapter facade for the hyperreal-backed csgrs core"
authors = ["Timothy Schmidt <timschmidt@gmail.com>"]
license = "MIT"
repository = "https://github.com/timschmidt/csgrs"
readme = "README.md"

[dependencies]
csgrs = { version = "0.23.0", path = "..", default-features = false }
hyperlattice = { version = "0.6.0", path = "../../hyperlattice" }
hyperreal = { version = "0.13.1", path = "../../hyperreal", default-features = false }

[features]
default = ["mesh", "sketch"]
mesh = ["csgrs/mesh"]
sketch = ["csgrs/sketch", "mesh"]

[[bench]]
name = "graphics_export"
harness = false
